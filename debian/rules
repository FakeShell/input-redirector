#!/usr/bin/make -f

PKG_DIR=debian/input-redirector

%:
	dh $@

execute_after_dh_install:
	# remove gschema compiled cache if it created
	rm -f ${PKG_DIR}/usr/share/glib-2.0/schemas/gschemas.compiled
